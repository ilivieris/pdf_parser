fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
mammoth>=1.8,<2.0
pdfplumber>=0.11,<1.0
pdf2image>=1.17,<2.0
pytesseract>=0.3.10,<1.0
openai>=1.50,<2.0
tiktoken>=0.7,<1.0

# Only used by app/services/semantic_analysis/ (ESCO skills retrieval) — remove these two
# lines too if that folder is ever removed. Embeddings themselves come from a remote API
# (see settings.py), so no local model / torch is needed — just the local vector index.
faiss-cpu>=1.8,<2.0
numpy>=1.26,<3.0
