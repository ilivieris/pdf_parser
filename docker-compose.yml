services:
  document-processor-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: document-processor-api:local
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
